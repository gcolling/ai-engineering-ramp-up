[project]
name = "ai-engineering-ramp-up"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Gabriel Colling", email = "colling.g2@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
ai-engineering-ramp-up = "ai_engineering_ramp_up:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "mypy>=2.3.1",
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
    "ruff>=0.16.5",
]
